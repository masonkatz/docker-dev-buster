version: "3"

services:
  buster-dev:
    build:
      context: ..
      dockerfile: centos8/Dockerfile
      args:
        - USER=${USER}
    image: devbox-centos8
    ports:
      - "22"
    volumes:
      - develop:/export

volumes:
  develop:
    external: true

